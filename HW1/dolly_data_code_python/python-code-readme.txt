Dependencies:
    -python3
    -pip3
    -numpy
    -pickle
    -time
    -sys
    -cv2
    -scipy

Instructions:
    -Simply run 'python3 SampleCameraPath.py' in the 'dolly_data_code_python/' directory and output text will start to show in the terminal. It will run through 7 images, displaying messages and the time it took to render each image when they are done. The images will be in the same directory as the code.
